# TraitTrawler Python dependencies
# The skill runs inside Claude Cowork, which installs these at session start.
# Install manually only if running the skill/scripts/ utilities standalone:
#   pip install -r requirements.txt

# Runtime: PDF text/table extraction. Used by pdf_ingest, verify_quote,
# triage_prefilter, pdf_peek, pdf_render, and pair_pdfs.
pdfplumber>=0.7.0

# Tests only: synthetic PDF generation for the grounding test
# (skill/tests/test_real_pdf_grounding.py). Not needed at runtime.
fpdf2>=2.7.0

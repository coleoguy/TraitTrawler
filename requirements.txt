# TraitTrawler Python dependencies
# These are used by the utility scripts (dashboard_generator.py, verify_session.py, export_dwc.py).
# The skill itself runs inside Claude Cowork and installs these automatically at session start.
# Install manually only if running scripts standalone:
#   pip install -r requirements.txt

pyyaml>=5.0
pdfplumber>=0.7.0
scipy>=1.9.0
matplotlib>=3.5.0
